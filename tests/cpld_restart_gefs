#
#  restart gefs test
#

source ${PATHRT}/tests/cpld_control_gefs

TEST_DESCR="Fully coupled FV3-CCPP-GOCART-MOM6-CICE-CMEPS-WW3 system - C384L127 MX025 - restart test"

FHROT_REST=9 #restart after any IAU
FHROT=$((FHROT+FHROT_REST))
RESTART_N=$((FHMAX-FHROT))
RESTART_INTERVAL="${RESTART_N} -1"
RESTART_FILE_SUFFIX_SECS="${SYEAR}-${SMONTH}-${SDAY}-$(printf "%05d" $(( (${FHROT}+ ${SHOUR})*3600 )))"
RESTART_FILE_PREFIX="${SYEAR}${SMONTH}${SDAY}.$(printf "%02d" $(( ${FHROT} + ${SHOUR} )))0000"
CICE_DIAGFREQ=`expr $((FHMAX-FHROT)) \* 3600 / $DT_CICE`

#Warm start settings already from "control" GEFS test
WW3_restart_from_binary='false'
#No increments/IAU in restart
READ_INCREMENT=.false.
IAU_INC_FILES="''"
IAU_DELTHRS=0
IAUFHRS=-1
IAU_OFFSET=0
ODA_INCUPD="False"
ODA_INCUPD_UV="False"

if [[ $MACHINE_ID = orion ]]; then
   WLCLK=20
fi
