###############################################################################
#
#  HRRR physics on 13km domain, restart run
#
###############################################################################

# This configuration is supposed to match the rrfs_dev1 parallel, so
# do not change the namelist without checking the current parallel.

TEST_DESCR="HRRR physics on 13km domain, restart run"

CNTL_DIR=conus13km_restart_mismatch

LIST_FILES="sfcf002.nc \
                   atmf002.nc"

export_hrrr_conus13km
FHROT=1
RESTART_FILE_PREFIX="${SYEAR}${SMONTH}${SDAY}.$(printf "%02d" $(( ${SHOUR} + ${FHROT}  )))0000"
RRFS_RESTART=YES
QUILTING_RESTART=.false.

if [[ ${MACHINE_ID} = ursa ]]; then
   EXCLUSIVE_NODES=.true.
fi 
